# React-form-Delete-Update-Save-LocalStorage
Created a web-app, in which user can submit form, the data of which will be stored in LocalStorage(as backend). User can edit the saved forms and Delete them
